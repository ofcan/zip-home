<%= form_for @post do |f| %>
  <%= f.text_field :title, :class => 'text_area', :placeholder => "title" %>
  <%= f.text_area :body, :rows => '18', :class => 'text_area', :placeholder => "content" %>
  <%= f.submit :class => 'btn btn-primary' %>
<% end %>
